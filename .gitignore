# Theos build artifacts
.theos/
obj/
packages/
*.deb

# Compiled dylibs (regenerated by `make` — never commit binaries).
# NOTE: do NOT match Makefile.dylib — it is a source Makefile, not a binary.
/*.dylib
.theos/**/*.dylib
packages/*.dylib

# Private development transcript (contains device info / proxy secrets — NEVER commit)
TG_PROXY_TWEAK.md

# macOS / OS junk
.DS_Store
*.log
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*~
